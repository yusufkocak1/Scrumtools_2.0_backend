# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/scrum_tools_db
spring.datasource.username=scrum_user
spring.datasource.password=scrum_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=kocak_Hayat_Iman_ve_Cihattir.1996
jwt.expiration=86400000

# Server Configuration
server.port=${PORT:8080}

# Application Configuration
spring.application.name=scrum-tools-backend

# Logging Configuration
logging.level.com.kocak.scrumtoolsbackend=DEBUG
logging.level.org.springframework.security=DEBUG
